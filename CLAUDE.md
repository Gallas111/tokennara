# tokennara — 에이전트 지침

이 파일은 2026-08-01에 신설했다. 이 레포만 CLAUDE.md가 없었고, 같은 날 **coinday와 primary 키워드가 완전히 겹치는 글이 11쌍** 발견됐다. 규칙이 메모리에만 있고 레포에 없으면 작업자가 못 본다.

## 정체성

- **코인 심화 매거진.** coinday(초보 가이드)·altnara(온체인/파생 지표)와 같은 운영자의 자매 사이트다.
- 도메인 `www.tokennara.com` · 배포 = GitHub Actions → Cloudflare Pages
- 글 디렉터리 `content/posts/` · 파일명 `<topic>-<section>-YYYY-MM-DD.mdx` (섹션 = `kimp` / `altpick` / `onchain`)

## 🔴🔴 집필 전 필수 — 교차 사이트 중복 게이트

coinday·tokennara·altnara는 **hard 클러스터**다. 같은 쿼리를 두 도메인이 겨냥하면 서로를 잡아먹고, 같은 운영자 지문(네트워크로 보이는 것)이 된다.
기존 게이트(`check-duplicate-post.py`·`check-keyword-overlap.py`)는 **같은 사이트 안만** 보므로 이 유형을 구조적으로 못 잡는다.

```bash
export PATH="/c/Users/owner/AppData/Local/Programs/Python/Python312:$PATH"
python ~/scripts/check-crosssite-overlap.py tokennara "<제목>" "<키워드1>" "<키워드2>"
```

- exit 0 = 작성 OK · exit 1 = 주의(각도 분리를 본문에 명시) · **exit 2 = 작성 금지, 다른 축으로 피벗**
- 🔑 **키워드를 2개 이상 넘겨라.** 제목만으로는 같은 의도를 못 잡는다.
- 🔑 키워드 후보는 상대 글의 `primary`뿐 아니라 **secondary·longTail까지** 읽고 골라라(실제로 secondary 충돌로 exit 2가 난 적이 있다).
- 클러스터 전수 감사: `python ~/scripts/check-crosssite-overlap.py --audit coin`

**실제로 벌어진 일(2026-08-01)**: coinday가 6월에 쓴 키워드를 이 레포가 2~4주 뒤 그대로 다시 쓰는 패턴이 한 달간 반복됐다. 22편이 같은 키워드로 경쟁해 28일 노출 36·클릭 0. 10편을 축 이동시키고 1편은 noindex 처리했다.

## 문체·자수

- 🔴 **합니다체.** 이 레포만 altnara와 함께 합니다체이고 나머지 7개 사이트는 해요체다. 일괄 지시를 그대로 따르지 말고 **최근 글을 열어 미러링**하라.
- 한글 **3,000자 이상** 권장, 게이트 컷은 2,500자. 깊이 분석 매거진 정체성 — 단순 양산 회피.
- 🔴 **상한을 두지 마라.** 2026-08-01 신설 당시 이 줄은 "3,000~4,000자 권장"이었는데 **실측과 어긋났다**. 최근 10편 중앙값 **5,144자**이고 8/1 발행분은 9,586자다(2026-08-03 실측). 그 문구 때문에 8/3 집필자가 "권장을 넘었다"고 불필요하게 플래그했다. 제약·트러블슈팅 축은 거래소별 조건을 표로 가르느라 길어지는 것이 정상이다.

```bash
bash ~/scripts/check-post-length.sh tokennara "<slug>"   # exit 0 나올 때까지 보강
```

⚠️ **파일 KB ≠ 한글 자수.** KB는 영문·이모지·MDX 마크업을 포함한다. 보고는 위 스크립트 출력값으로만.

## 토픽 선정

- 🔴 **검색량 0인 딥다이브 토픽 금지.** 과거 28일 노출 164(글당 1.1)의 원인이 "맨틀 딥다이브"·"실현시총" 같은 무수요 토픽이었다.
- **수요형 실용 쿼리**로 간다 — 김프 계산, 거래소·출금 수수료 비교, 에어드랍 일정, 스테이킹 이자, 상장 일정, 테더 환전 등. deepdive는 **주 1편 이하**.
- 🔴 그렇다고 coinday의 초보 헤드 쿼리로 내려가지 마라. 그게 위 11쌍 사고의 원인이다. 같은 주제를 다루더라도 **제약·트러블슈팅·판별 기준** 처럼 한 겹 더 들어간 축을 잡아라.

## 🔴 하지 말 것

- **사이트 간 직접 링크 금지.** coindaynow.com·altnara.com을 본문에 링크하거나 언급하지 마라(PBN 회피).
- 매수·매도 권유, 목표가, 승률, "100배" 류 표현 금지. 면책 문구 유지.
- 가짜 "LIVE" 표기·실시간처럼 보이는 고정 수치 위젯 금지.
- 커밋된 슬러그를 `GATE_SLUGS`에 넣지 마라(`scripts/publish-gate.ts` 엄격모드 오격리 → 라이브 글 삭제 경로).
- 제목에 월 표기(`(8월 기준)` 등) 금지 — 매달 낡아 SERP에서 걸러진다. 기준 시점은 본문에.

## MDX 규칙

- 본문 중괄호 `{}` 금지 · self-closing 태그 금지 · FAQ의 `q`·`a` 값은 따옴표로 감쌀 것
- 내부 링크는 **실존 슬러그만**. 🔴 제목을 바꾸면 그 글을 가리키는 다른 글의 앵커 텍스트가 낡는다 — `grep -rl "/blog/<슬러그>" content/`로 함께 고쳐라.
- `.research-cache/<slug>.md`는 **버전관리 대상이다**(2026-08-01부터). 팩트 검증이 "캐시 vs 원문" 대조라 캐시가 없으면 재검증이 불가능하다. 각 `확인된 사실` 줄에 근거 URL을 붙여라.

## 도메인 사실 (자주 틀리는 것)

- 실명확인 입출금계정 제휴은행(2026년 8월 초 기준): 업비트=케이뱅크 · **빗썸=KB국민**(2025-03 NH농협에서 전환) · 코인원=카카오뱅크 · 코빗=신한 · 고팍스=전북. 🔴제휴 계약 만료가 잇따라 예정돼 있어 바뀐다 — 기준 시점과 변동 가능성을 함께 적어라.
- 업비트 출금 제한은 **방향이 두 개**다. ①원화 첫 입금 → 72시간 디지털자산 출금 제한 ②디지털자산 첫 입금 → 72시간 **원화** 출금 지연 ③두 번째 원화 입금부터 건별 24시간 지연제. ①만 적으면 외부에서 코인 받아 파는 독자를 오도한다. 🔴이건 **업비트 기준**이다 — 빗썸은 "출금 지연 기간 중에도 원화 출금은 자유"라 방향이 반대다.
- 코인 상장폐지에는 주식의 **정리매매 제도가 없다.** 거래소가 공지한 "거래지원 종료 일시"까지 평소대로 거래되다 멈출 뿐이다. 주식은 가격제한폭 해제·단일가 매매 같은 별도 규칙이 붙지만 코인은 아니다.
